# Local Cordis composition (config-list dialect) for the standalone loader:
#
#   npm run dev     (node --import tsx scripts/loader.mjs)
#
# The harness install format is the bundle layer cordis.patch.yml instead —
# this file exists for zero-harness development and CI smoke runs.
- name: './src/index.ts'
